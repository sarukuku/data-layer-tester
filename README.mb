# automated-dl-test

A Cypres configuration that's been programmed to test the data layer object a bunch of tommy.com sites.

## How to use?
1. Clone / download this repository
2. Run `npm install` in the root folder
3. Run `npm run cypress:open` in the root folder to start Cypress
4. Click on "Run all specs" button on the UI that opens after running the previous command